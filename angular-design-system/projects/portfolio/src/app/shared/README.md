# Shared

Application-private reusable UI, directives, pipes and utilities belong here only after at least two
Portfolio features need them. Reusable brand UI remains in `gh-design-system`; this directory must
not become a duplicate component library.
